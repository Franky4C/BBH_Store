<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    android:orientation="vertical"
    tools:context=".DetalleProducto">

    <!-- 1. CABECERA: Mismos elementos, estilo limpio -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="8dp">

        <ImageButton
            android:id="@+id/btn_regresar_detalle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/icon_04"
            app:tint="@color/white" />

        <TextView
            android:id="@+id/usuario_detalle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Nombre Cliente"
            android:textStyle="bold"
            android:textSize="18sp"
            android:textColor="#E0E0E0"
            android:fontFamily="sans-serif-condensed"
            android:gravity="center"/>

        <ImageButton
            android:id="@+id/btn_carrito_detalle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/icon_carrito"
            app:tint="@color/white" />
        <TextView
            android:id="@+id/contador_carrito_detalle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0"
            android:textSize="14sp"
            android:textStyle="bold"
            android:layout_marginStart="4dp"
            android:gravity="center_vertical" />
    </LinearLayout>

    <!-- 2. SCROLLVIEW CON EL CONTENIDO PRINCIPAL -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- IMAGEN DEL PRODUCTO: Sin distorsión, dentro de una tarjeta -->
            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardCornerRadius="16dp"
                app:cardBackgroundColor="#1C1C1C">

                <ImageView
                    android:id="@+id/imagen_detalle"
                    android:layout_width="match_parent"
                    android:layout_height="300dp"
                    android:scaleType="centerCrop"
                    android:src="@drawable/artesania1" />
            </com.google.android.material.card.MaterialCardView>

            <!-- DETALLES DEL PRODUCTO: Textos estilizados -->
            <TextView
                android:id="@+id/nombre_detalle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Nombre del producto"
                android:textSize="28sp"
                android:textStyle="bold"
                android:textColor="@color/white"
                android:layout_marginTop="24dp"
                android:layout_marginBottom="12dp"/>

            <TextView
                android:id="@+id/descripcion_detalle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Descripción del producto"
                android:textSize="16sp"
                android:textColor="#C5C3C3"
                android:lineSpacingExtra="4dp"/>

            <!-- PRECIO: Estilo prominente -->
            <TextView
                android:id="@+id/precio_detalle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="$ 9,999.99"
                android:textSize="32sp"
                android:textColor="@color/white"
                android:textStyle="bold"
                android:layout_marginTop="24dp"
                android:fontFamily="sans-serif-condensed"
                android:textAlignment="textEnd"/>

            <!-- BOTÓN DE COMPRA: Estilo moderno -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_comprar_detalle"
                style="@style/Widget.Material3.Button"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="COMPRAR"
                android:paddingVertical="14dp"
                android:textSize="16sp"
                app:backgroundTint="#4CAF50"
                app:cornerRadius="12dp"
                android:layout_marginTop="32dp"/>

            <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_agregar_carrito_detalle"
            style="@style/Widget.Material3.Button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="AGREGAR A CARRITO"
            android:paddingVertical="14dp"
            android:textSize="16sp"
            app:backgroundTint="#4CAF50"
            app:cornerRadius="12dp"
            android:layout_marginTop="32dp"/>

        </LinearLayout>
    </ScrollView>
</LinearLayout>
