<?xml version="1.0" encoding="utf-8"?>
<!-- 1. El layout raíz ahora es un LinearLayout. Es más eficiente. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#191919"
    tools:context=".Tienda">

    <!-- 2. Barra Superior: Mejor alineación y efecto visual en botones -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="12dp">

        <ImageButton
            android:id="@+id/btn_cerrar_sesion_tienda"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/icon_04"
            android:padding="8dp"
            android:contentDescription="Regresar al menú"
            app:tint="@android:color/white" />

        <TextView
            android:id="@+id/usuario_tienda"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:textColor="@android:color/white"
            android:textSize="20sp"
            android:textStyle="bold"
            tools:text="Hola, Usuario" />

        <ImageButton
            android:id="@+id/btn_carrito_tienda"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/icon_carrito"
            android:padding="8dp"
            android:contentDescription="Ver carrito"
            app:tint="@android:color/white" />

    </RelativeLayout>

    <!-- Título de la sección -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Nuestros Productos"
        android:textColor="#8AFF8E"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="8dp"/>

    <!-- 3. RecyclerView optimizado: Ocupa el resto de la pantalla y se encarga del scroll -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/artesanias_tienda"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp" />

</LinearLayout>
